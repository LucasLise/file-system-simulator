<%= simple_form_for @usuario do |f| %>
  <div class="panel panel-default animated rollIn">
    <div class="panel-body">
      <%= f.input :nome, class: 'sr-only', label: false, placeholder: 'Usuário'%>
      <%= f.button :submit, 'Acessar', class: 'btn btn-md btn-primary btn-block', style: 'background: #3f5d7d; color: #FFF' %>
    </div>
  </div>
</div>
<% end %>
